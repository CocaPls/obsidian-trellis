{
	"name": "trellis-plugin",
	"version": "0.1.0",
	"description": "Tag-driven identifier sync for Obsidian — location tag → filename prefix (trekey).",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "node --test"
	},
	"keywords": ["obsidian", "plugin", "tag", "rename", "trellis"],
	"author": "CocaPls",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.0.0",
		"builtin-modules": "^4.0.0",
		"esbuild": "^0.24.0",
		"obsidian": "latest",
		"tslib": "^2.8.0",
		"typescript": "^5.7.0"
	}
}
